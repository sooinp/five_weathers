# FastAPI 핵심
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# DB
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# 설정
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# 인증 — bcrypt 4.0.1 고정 (passlib 1.7.4 호환)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Rate Limiting
slowapi>=0.1.9
packaging>=24.0

# 데이터 처리
pandas>=2.2.0
python-dateutil>=2.9.0
pyarrow>=15.0.0
numpy>=1.26.0
scipy>=1.12.0

# 지형 PNG 생성 (Leaflet imageOverlay용)
Pillow>=10.0.0

# 선택: GeoTIFF 로드 (risk map 파일 처리 시)
# rasterio>=1.3.0

# 테스트
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
# httpx-ws>=0.6.0  # WebSocket 테스트 시 활성화
